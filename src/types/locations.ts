
export interface TopLocation {
  id: string;
  name: string;
  address: string;
  city: string;
  state: string;
  type: string;
  rating: number;
  searches: number;
  percentChange: number;
  contact?: string;
}
